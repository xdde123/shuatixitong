package com.guanlixitong.app;
import android.content.*;
public class BootReceiver extends BroadcastReceiver{ @Override public void onReceive(Context c,Intent i){ /* 提醒时间保存在 Web 数据中，首次打开 App 后会重新注册每日提醒。 */ } }
